{jshead}

// Uncompressed source code:
// https://github.com/magicoflolis/twitter-translator/src
'use strict';
(() => {
  const twCSS = `{foreign}`,
  tetCSS = `{tetCSS}`,
  nitterCSS = `{nitterCSS}`,
  debugToggle = {debugToggle};
  {languages}
  {code}
})();
